#ifndef COLUMBITS_VECTOR_H
#define COLUMBITS_VECTOR_H

#include "COLUMBITS.h"

typedef struct {} _RAY;
typedef struct {} _POLYGON;

_BITMAP* DRAW_LINE();
_BITMAP* DRAW_TRIANGLE();
_BITMAP* DRAW_POLYGON();

#endif // COLUMBITS_VECTOR_H